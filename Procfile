# Build.io / Heroku-style process declaration.
# `vite build` (via npm heroku-postbuild) emits static assets to ./dist,
# and this thin Express server serves them on the platform's $PORT.
web: node server.js
